interface:
  display_name: "Arena"
  short_description: "Compare independent candidates and synthesize"
  default_prompt: "Use $arena to compare isolated candidates, run the required blind judge, and synthesize the strongest result."
