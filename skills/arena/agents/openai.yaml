interface:
  display_name: "Arena"
  short_description: "Compare independent candidates and synthesize"
  default_prompt: "Use $arena to compare independent approaches and synthesize the strongest result."
